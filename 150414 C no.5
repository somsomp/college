#include <stdio.h>
void main(){
  float line, height;
  float area = 0;
  printf("삼각형의 밑변과 높이를 입력하시오 : ");
  scanf("%f %f", &line, &height);
  
  area = line * height * 0.5;
  
  printf("삼각형의 넓이는 %f이다.\n", area);
}
